import React from 'react';

export default function Footer() {
  return (
    <footer style={{
      background: 'var(--text-dark)',
      color: 'var(--off-white)',
      fontFamily: 'var(--font)',
      padding: '2rem 10vw',
      textAlign: 'center',
    }}>
    </footer>
  );
}
